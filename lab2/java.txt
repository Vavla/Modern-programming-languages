public class Person {
    private String name = "";
    private int age = 0;
    public Person(String name,int age){
        this.name = name;
        this.age = age;
    }
    public String getName(){
        return name;
    }
    public int getAge(){
        return age;
    }
    public String getAgeCategory() {
        if (age < 0) {
            return "Некорректный возраст";
        } 
        else if (age < 13) {
            return "ребёнок";
        } 
        else if (age < 18) {
            return "Подросток";
        } 
        else if (age < 60) {
            return "Взрослый";
        } 
        else {
            return "Пожилой";
        }
    }
}

public class Cat {
    private String name = "";
    public Person(String name){
        this.name = name;
    }
    public String getName(){
        return name;
    }
}
public class Dog {
    private String name = "";
    public Person(String name){
        this.name = name;
    }
    public String getName(){
        return name;
    }
}
public class MyClass{ 

    public String get(){
    return name;
    }
    public static void main (String args[]){
          
        Person p = new Person("Anna", 24);
        Person p2 = new Person("Vladimir",23);
        System.out.println(p.getAgeCategory());
        System.out.println("Hello, Person!");
        if (p.name == "Anna" && p2.name == "Vladimir") {
            System.out.println("Ее зовут Анна");
            System.out.println("Его зовут Владимир");
        }
        if(p2.getAge() > p.getAge()){
            System.out.println(p2.getName() + " старше " + p.getName());
        }
        else {
            System.out.println(p2.getName() + " младше " + p.getName());
        }
        Cat cat = new Cat("Kitty");
        System.out.println("Cat'name - " + cat.getName());
        cat.name = "Lili";
        System.out.println("Cat'name - " + cat.getName());

    }
}